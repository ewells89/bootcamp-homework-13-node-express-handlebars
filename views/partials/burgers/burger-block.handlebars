<li>
	{{name}}

	<button class="btn btn-dark change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devour}}DEVOURED!{{else}}NOT DEVOURED!{{/if}}
	</button>

	<button class="btn btn-dark delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
